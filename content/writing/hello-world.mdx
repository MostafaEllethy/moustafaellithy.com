---
title: "Hello, World"
description: "A dummy post exercising headings and syntax-highlighted code blocks."
date: "2026-01-15"
tags: ["meta", "test"]
draft: false
---

## Why this post exists

This post is placeholder content used to exercise the MDX rendering pipeline: headings, paragraphs, and syntax-highlighted code blocks in five languages.

### A TypeScript example

```ts
function greet(name: string): string {
  return `Hello, ${name}!`;
}

console.log(greet("World"));
```

### A TSX example

```tsx
function Greeting({ name }: { name: string }) {
  return <p>Hello, {name}!</p>;
}
```

### An HTML example

```html
<section>
  <h1>Hello, World</h1>
  <p>This is a placeholder.</p>
</section>
```

### A CSS example

```css
.greeting {
  color: #171717;
  font-weight: 600;
}
```

### A bash example

```bash
echo "Hello, World"
```

## Wrapping up

That covers headings, paragraphs, and code blocks across the languages this pipeline supports.
